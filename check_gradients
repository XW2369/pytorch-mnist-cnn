import numpy as np
import torch
from torchlab.autograd_lab import forward_mlp, backward_mlp, check_gradients


def test_W1_grad_match():
    errs = check_gradients()
    assert errs[1] < 1e-9, f"W1 err={errs[1]}"


def test_W2_grad_match():
    errs = check_gradients()
    assert errs[3] < 1e-9, f"W2 err={errs[3]}"


def test_bias_grad_match():
    errs = check_gradients()
    assert errs[2] < 1e-9 and errs[4] < 1e-9

def test_zero_grad_when_y_eq_z2():
    # numpy 造数据 → forward_mlp 拿 z2 → y = z2.copy()
    # backward_mlp 得 4 个梯度 → 断言全部 |grad| < 1e-9
